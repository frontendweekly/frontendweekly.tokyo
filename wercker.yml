box: wercker/nodejs
build:
  steps:
    - arjen/hugo-build:
        version: 0.14
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GITHUB_TOKEN
        domain: frontendweekly.tokyo
        basedir: public
